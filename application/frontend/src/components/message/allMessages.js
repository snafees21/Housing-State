const allMessages = ({ messages }) => {
  const handlestuff =()=>{
    console.log("bruh");
  }
  return (
    <div className="card-body">
       <a href="#" class="list-group-item list-group-item-action" onClick={()=>handlestuff()}>
        <ul>
          <legend> Jon Bruh </legend>
          <p>Hi, I really this house, is it still availible??? reeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee</p>
        </ul>
        </a>
    </div>
  );
};
export default allMessages;
